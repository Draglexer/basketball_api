export enum UserAmpluaType {
    forward ,
    defender,
    center
}
export enum UserGenderType {
    man,
    woman
}
export enum UserRoleTypes {
    guest,
    participant,
    organizer,
    admin
}